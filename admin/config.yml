backend:
  name: github
  repo: z08id-coder/bio
  branch: main

media_folder: "images"
public_folder: "images"

collections:
  - name: "posts"
    label: "البوستات"
    folder: "_posts"
    create: true
    delete: true
    # الترتيب التلقائي بناءً على وقت النشر بالثانية لضمان ظهور الأحدث فوق
    slug: "{{year}}-{{month}}-{{day}}-{{hour}}-{{minute}}-{{slug}}"
    fields:
      - {label: "اسم الشخص", name: "title", widget: "string"}
      
      # التاريخ مخفي ويؤخذ تلقائياً لحظة الضغط على Publish
      - {
          label: "التاريخ", 
          name: "date", 
          widget: "hidden", 
          default: "{{now}}"
        }

      # حقل الأفاتار (الصورة الدائرية)
      - {label: "الأفاتار (صورة الشخص)", name: "avatar", widget: "image", required: true}
      
      # حقل صورة المنشور (اختياري)
      - {label: "صورة البوست (اختياري)", name: "image", widget: "image", required: false}
      
      - {label: "نص المنشور", name: "body", widget: "markdown"}

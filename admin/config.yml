backend:
  name: git-gateway
  repo: z08id-coder/bio
  branch: main

media_folder: "images"
public_folder: "images"

collections:
  - name: "posts"
    label: "البوستات"
    folder: "_posts"
    create: true
    delete: true # هاد السطر بخليك تحذف أي بوست بدك ياه من اللوحة
    fields:
      - {label: "اسم الشخص", name: "title", widget: "string"}
      - {
          label: "التاريخ", 
          name: "date", 
          widget: "datetime", 
          date_format: "DD/MM/YYYY", 
          time_format: "hh:mm A", 
          format: "YYYY-MM-DD hh:mm A",
          picker_utc: false
        }
      # حقل الأفاتار (الصورة الدائرية اللي جنب الاسم)
      - {label: "الأفاتار (صورة الشخص)", name: "avatar", widget: "image", required: true}
      # حقل صورة المنشور (بتصير اختيارية زي ما طلبت)
      - {label: "صورة البوست (اختياري)", name: "image", widget: "image", required: false}
      - {label: "نص المنشور", name: "body", widget: "markdown"}
